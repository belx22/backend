# Build
/target
/bin

# Donnees d'execution locales : captures faciales / pieces d'identite televersees
# (donnees personnelles sensibles) et surcharge Docker propre au poste.
/data/
docker-compose.override.yml

# SonarQube — token local + dossier de travail du scanner, ne pas versionner
sonar-project.properties
.scannerwork/

# Secrets — NE JAMAIS versionner
.env
*.log
bello
bello.pub
*.pem
*.key
id_rsa*
